apiVersion: v1
kind: Service
metadata:
  name: {{ include "etfinsight_backend.fullname" . }}
  labels:
    {{- include "etfinsight_backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "etfinsight_backend.selectorLabels" . | nindent 4 }}
